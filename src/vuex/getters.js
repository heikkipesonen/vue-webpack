const getters = {
  /**
   * getter
   * @param  {[type]} state [description]
   * @return {[type]}       [description]
   * @example
   * getterName (state) {
   *  return state.something
   * }
   */
}

export default getters
