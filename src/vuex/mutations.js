const mutations = {
  /**
   * mutation
   * @param {object} state
   * @param {payload} payload
   * @example
   * setView (state, payload) {
   *  state.payload = payload
   * }
   */
}

export default mutations
